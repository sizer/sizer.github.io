{
  "name": "sizer.github.io",
  "version": "1.0.0",
  "description": "Hi, this is sizer.",
  "author": "sizer",
  "private": true,
  "scripts": {
    "deploy": "yarn run build && gh-pages -d dist -e docs -b master",
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "yarn run lint"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "babel-eslint": "^8.2.1",
    "eslint": "^5.0.1",
    "eslint-config-prettier": "^3.1.0",
    "eslint-loader": "^2.0.0",
    "eslint-plugin-prettier": "2.6.2",
    "eslint-plugin-vue": "^4.0.0",
    "gh-pages": "^2.0.1",
    "nodemon": "^1.11.0",
    "prettier": "1.14.3"
  }
}
